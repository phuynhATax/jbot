services:
  postgres:
    image: ankane/pgvector
    container_name: pd_chatbot_postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: chatbotdb
    ports:
      - "9432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  pgadmin:
    image: dpage/pgadmin4
    container_name: pd_chatbot_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@local.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "9080:80"
    volumes:
      - ./pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres

  n8n:
    image: n8nio/n8n
    container_name: pd_chatbot_n8n
    restart: always
    environment:
      - GENERIC_TIMEZONE=America/Los_Angeles
    ports:
      - "9678:5678"
    volumes:
      - n8n_data:/home/node/.n8n

  chatbot:
    build: ./chatbot
    container_name: pd_chatbot_node
    restart: always
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      DATABASE_URL: postgres://admin:admin@postgres:5432/chatbotdb
      PORT: 9000
    ports:
      - "9000:9000"
    depends_on:
      - postgres

  chatbot_ui:
    build: ./chatbot-ui
    container_name: pd_chatbot_ui
    environment:
      - REACT_APP_API_URL=https://rssbot.ataxadmin.com/
    ports:
      - "9001:80"
    depends_on:
      - chatbot

volumes:
  postgres_data:
  pgadmin_data:
  n8n_data:
